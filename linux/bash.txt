"info $name" 	双引号 里面的$name输出变量的结果
'info $name'	单引号 里面的$name原样输出
`cmd`		反斜扛 运行命令的结果
cmd1 && cmd2 || cmd3 cmd1运行成功就运行cmd2，否则运行 cmd3

shell 数组 
	声明 declare -a name
	追加 for i in {1..3};do
		name+=($i)  //加()
	     done 	
